doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        title Gallery Page
    body
        h3 Gallery Loaded
        h4 Click to update server time
        form(name="timePost", action="/time", method="POST")
            input(type="submit")